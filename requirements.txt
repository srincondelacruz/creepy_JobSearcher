# Core
python-telegram-bot>=21.0
APScheduler>=3.10.4
pytz>=2024.1

# LangGraph agent (Azure OpenAI backend)
langgraph>=0.2.40
langgraph-checkpoint-sqlite>=2.0.0   # persistent cross-session memory
langchain-openai>=0.2.0              # AzureChatOpenAI
langchain-core>=0.3.0

# Optional — only if switching back to Anthropic
# anthropic>=0.40.0
# langchain-anthropic>=0.3.0

# Scraping / URL fetching
requests>=2.32.0
beautifulsoup4>=4.12.3
lxml>=5.2.0         # faster HTML parser for bs4
playwright>=1.44.0  # JS-rendered offer pages — run: playwright install chromium

# Config & utils
PyYAML>=6.0.2
python-dotenv>=1.0.1

# CLI & display
click>=8.1.7
rich>=13.9.0
loguru>=0.7.2
